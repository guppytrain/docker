# general
USER="$DOCKERID"

# build time
# folders; from the context of <DOCKER>/<FLAVOR>_dev/.
TMP_TGT="tmp"
TMP_SRC="../common"
BASE_CMD_DIR="../cmds"
CMD_DIR="./cmds"

# misc
TAG_PREFIX="current_"
BUILT_FILE=".built"

# post build, shell related, etc
BASH_CONFIG_FILE="./CONFIG.bash"
VOLUME_CONFIG_FILE="./CONFIG.volume"
SHARE_CONFIG_FILE="${SHARE_DIR}/etc/include/.docker"
