RUN_LATEST=". ../CONFIG.volume; sudo docker run -it -v $SHARED_VOLUME:$CONTAINER_VOLUME <IMG> bash"
START_LATEST=". ../CONFIG.volume; sudo docker container start -i $(sudo docker container ls -lqa -f ancestor=<IMG>)"
